<h1>Articles</h1>
<ul>
   <% @articles.each do |article| %>
    <li>
      Title: <%= article.title %>
      <p>
        <%= article.body.length > 10 ? article.body.slice(0..10) + '...' : article.body %>
      </p>
    </li>
   <% end %>
</ul>
